name: MyKos
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  http: ^1.1.0
  provider: ^6.0.5
  google_fonts: ^6.1.0
  cached_network_image: ^3.3.0
  url_launcher: ^6.3.2
  share_plus: ^13.1.0
  # webview_flutter: ^4.4.0
  flutter_map: ^6.1.0
  latlong2: ^0.9.0
  shared_preferences: ^2.5.5
  # google_sign_in: ^6.1.5
  firebase_core: ^4.0.0
  firebase_auth: ^6.0.0
  nsd: ^5.0.1        # mDNS service discovery (Android NSD / iOS Bonjour) [citation:2]
  shelf: ^1.4.0      # HTTP server untuk backend
  shelf_web_socket: ^3.0.0  # WebSocket support
  # http: ^1.1.0       # HTTP client (udah punya)
  # network_info_plus: ^4.0.0
  google_sign_in: ^5.4.0

  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/images/logo_vertikal_mykos.png